import java.util.Scanner;

public class InsertNodeRecursively {
	
//	public static void Reverse(Node<Integer> head) {
//
//		if(head==null) {
//			return;
//		}
//		else { 
//			Reverse(head.next);
//	        System.out.print(head.data+" ");
//	    }
//		
//	}
//		
	public static void InsertNode(Node<Integer> head ,int pos,int data) {
	 if(pos == 0) {
			Node<Integer> newnode = new Node<Integer>(data);
			newnode.next = head;
			IntserR(newnode);
		}
	}
	public static Node<Integer> IntserR(Node<Integer>head){
       if(head==null) {
    	   return head;
       }
		
		return IntserR(head.next);
	
	}
		public static void PrintLinked(Node<Integer> head) {
			while(head!=null)
			{
				System.out.print(head.data+ " ");
				head = head.next;
			}
			
		}
		public static Node<Integer> takeInput(){
			Scanner s = new Scanner(System.in);
			int data = s.nextInt();
			Node<Integer> head = null,tail = null;
			while(data!=-1) {
				Node<Integer> newNode = new Node<Integer>(data);
				if(head==null) {
					head = newNode;
					tail = newNode;
				}
				else {
					tail.next = newNode;
					tail = tail.next; // tail = currentNode
				}
				data = s.nextInt();
				}
			return head;
		}
		
		public static void main(String args[]) {
			Scanner s = new Scanner(System.in);
			Node<Integer> head = takeInput();
			InsertNode(head,0,10);
			
			//System.out.println();
			//Reverse(head);
			//PrintLinked(head);
			
			
		}
	}


